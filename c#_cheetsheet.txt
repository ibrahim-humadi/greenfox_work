
Division with decimal places:
double mynumber = 10.0 / 3.0;
Console.WriteLine(mynumber);


//add commas between array numbers.
string commaSeperatedNumbers = String.Join(',', numbers);

return commaSeperatedNumbers;

console.writeline(ArrayToString(numbers));

// [1,2,3,4] to array of int
string[] strValues = str.Split(',');

strValues[0] = strValues[0].Substring(1);

for each (var strValue in strValues)
	{
	Consolw.WriteLine(strValue);
	}

// "hello"
Substring (1) // would give "ello"
Substring (2) // would give "llo"



Remainder:
double mynumber = 10.0 % 3.0;
Console.WriteLine(mynumber);


highlight lines and hold alt to move lines.

cntrl / to comment out a block of code
cntrl L or cntrl x to delete line 


for (int star = 0; star <= rowstar; star++)
				{
					Console.Write("*");
				}

error message:

throw new Exception("my error message");

try {
	method
} catch (Exception error1)
{
	Console.Error.WriteLine(error1);
	Console.WriteLine("I dont care about this error");
}

wrtie text to a file

File.WriteAllText("./output.txt", "Hello World");		// new file and overwite if already exists
File.AppendAllText("./output.txt, "Hello Again");		// add to end of the file
File.AppendAllText("./output.txt, "n/Hello Again");		// add to end of the file in new line

Console.WriteLine($"I dont care about this error" + path);

//Read all text
string filecontent = File.ReadAllText("./output.txt");

// read all lines
string[] 


// streamReader
// streamWriter
//uses System.IO

streamReader sr = new streamReader ("./output.txt")

prop + tab +tab = constructor

propfull + tab + tab = field

alt + shift + enter = fullscreen

